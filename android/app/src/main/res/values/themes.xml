<?xml version="1.0" encoding="utf-8"?>
<!--
    Offline Emergency Mesh Communication System
    Theme Resources — Material Design 3 XML theme definitions

    Primary usage: splash screen, activity window background, notification styling.
    The actual Compose UI uses MeshTheme (Kotlin) from core/designsystem/theme/Theme.kt.
-->
<resources>

    <!-- Base application theme — Material3 parent -->
    <style name="Theme.OfflineEmergencyMesh" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/mesh_primary</item>
        <item name="colorOnPrimary">@color/mesh_on_primary</item>

        <!-- Error color -->
        <item name="colorError">@color/mesh_error</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/mesh_background_light</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Edge-to-edge support -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Splash screen theme — used before Compose initializes -->
    <style name="Theme.OfflineEmergencyMesh.Splash" parent="Theme.SplashScreen">
        <!-- Deep indigo splash background (dark-by-default, night override in values-night/) -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Shield + mesh logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <!-- Branded indigo circle behind the icon on the system splash -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <!-- Horizontal mesh network mark at the bottom (Android 12+) -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/ic_splash_brand</item>
        <item name="postSplashScreenTheme">@style/Theme.OfflineEmergencyMesh</item>
    </style>

</resources>
